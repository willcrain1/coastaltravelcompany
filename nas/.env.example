# Copy to .env and fill in values — never commit .env
# Get the tunnel token from: Cloudflare Zero Trust dashboard → Networks → Tunnels → your tunnel → Configure → Token

TUNNEL_TOKEN=your-cloudflare-tunnel-token-here
